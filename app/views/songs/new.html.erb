<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist.name %>
  <%= f.text_field :artist.name %>
  <%= f.collection_select :genre, Genre.all, :id, :name %>

  <%= f.submit %>
<% end %>
